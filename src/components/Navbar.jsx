import React from 'react';

function Navbar(){
  return (
    <div>
      <button>Home</button>
      <button>Notifications</button>
      <button>Messages</button>
    </div>
  )
}

export default Navbar;
